<div class="container">
  <div class="d-flex flex-column justify-content-center align-items-center">
    <div class="img-name">
      <% if yacht.photo.attached? %>
        <%= cl_image_tag(yacht.photo.key, class:"card-img-top rounded-4") %>
      <% end %>
    </div>

    <div>
      <h4 class="my-2"><%= yacht.name.capitalize %></h4>
      <p class="mb-0"><%= yacht.address %></p>
      <p class="mb-0">£<%= yacht.price %> per day</p>
    </div>

  </div>
</div>
